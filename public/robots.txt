# robots.txt for React DataGrid
# Allow all search engines to crawl the site

User-agent: *
Allow: /

# Sitemap location
Sitemap: https://reactdatagrid.dev/sitemap.xml

# Crawl-delay (optional, prevents overloading server)
Crawl-delay: 1
